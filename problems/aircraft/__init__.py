"""Paper #1: aircraft-level positioning problem.

Self-contained.  Read ``problem_statement.md`` for the operational and
formal description.  ``checker.py`` enforces the requirements; the
instance schema lives in ``instance_schema.json``; instances under
``instances/``.
"""
