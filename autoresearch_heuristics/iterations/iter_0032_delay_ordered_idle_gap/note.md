# iter_0032_delay_ordered_idle_gap — process aircraft in delay-DESC order for Op 4

Same score +0.0191.  Delay-ordering tries the same aircraft first but the
first-improvement criterion gives equal results since the set of improving
moves is the same; only the order they're discovered differs.

**rejected** (no improvement).
