# iter_0049_max_needed_delay — compute max needed delay across overlapping fronts

Result: identical score (sequential and max-needed converge to same total
because LS iterates max_iter times until convergence).

**rejected** (no functional improvement).
