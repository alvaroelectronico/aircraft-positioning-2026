"""Hand-coded solving method.

Houses every MILP formulation (gurobipy + pyomo) and every hand-written
heuristic that pre-dates the autoresearch / LLM-iterative work (FAS,
TGR, LNS, topology, constructive).  Organised per problem under
``aircraft/`` and ``jobs/``.
"""
